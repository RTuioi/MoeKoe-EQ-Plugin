name: Archive Traffic

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - uses: groda/github-traffic-archiver@v2
        with:
          metrics-repo: 'RTuioi/traffic-vault'
          metrics-pat: ${{ secrets.METRICS_PAT }}